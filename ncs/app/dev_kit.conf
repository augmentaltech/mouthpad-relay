# Dev-kit overlay for the vox_dotto app: the standalone KTD2026 evaluation board
# uses I2C address 0x30, whereas the production Dotto part is 0x32 (the default).
# Build for the dev kit with: -DEXTRA_CONF_FILE=dev_kit.conf
CONFIG_KTD2026_I2C_ADDR=0x30
